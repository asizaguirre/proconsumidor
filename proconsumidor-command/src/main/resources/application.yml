spring:
  application:
    name: proconsumidor-command
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=ProConsumidorDB;encrypt=true;trustServerCertificate=true
    username: sa
    password: ProConsu@2026QA
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.SQLServerDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
    listener:
      simple:
        acknowledge-mode: auto
        concurrency: 5
        max-concurrency: 10

logging:
  level:
    br.gov.mj.senacon: DEBUG
